<%= image_tag @product.image.url(:medium) %>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

<p>
  <strong>Price:</strong>
  <%= number_to_currency(@product.price) %>
</p>

<% if current_user != @product.user %>
  <%= link_to "Buy It Now", new_product_order_path(@product) %>
<% end %>

<%= link_to 'Back', products_path %>
